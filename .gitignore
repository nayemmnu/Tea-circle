# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
build/
.flutter-plugins
.flutter-plugins-dependencies
.metadata
pubspec.lock
.idea/
*.iml

# The android/ folder is generated by the CI workflow
/android/
/ios/

# Firebase CLI
.firebase/
firebase-debug.log

# Push key: written by CI from the FCM_SERVICE_ACCOUNT secret. NEVER commit it.
/assets/fcm_service_account.json

# NOTE: google-services.json is intentionally NOT ignored (CI needs it).
# Keep the repository private.
